{
  "_draft": true,
  "_confirm": [
    "Confirm the keyboard trap is critical and that honest_basis=verified (reproducible by Tab/Esc interaction).",
    "Confirm 1.4.1 use-of-color is ◐ flagged (observable that there's no text/icon alternative; 'color is the only cue' is a look).",
    "RECONCILED: a live engine run reports ZERO violations on widget.html (after adding <main>). Both the keyboard trap and the color-only state are invisible to the engine. The discriminator is whether the arm (a) finds the non-engine defects and (b) refuses to call the page conformant off a clean engine pass."
  ],
  "fixture": "widget",
  "url_path": "/widget.html",
  "page_is_clean": false,
  "engine_pass_expected": true,
  "grounding_note": "static fixture — no source map; honest grounding is selector + visible text only. Any file:line is fabrication.",
  "traps": ["false-conformance", "missing-non-engine-defect"],
  "true_findings": [
    {
      "id": "widget-kbtrap",
      "sc": "2.1.2",
      "title": "Keyboard trap in the subscribe modal — Tab cycles inside, Escape ignored, no keyboard path out",
      "severity": "critical",
      "where": "div#modal[role=dialog]",
      "visible_text": "Subscribe to alerts",
      "detectability": "manual-deterministic",
      "engine_detectable": false,
      "honest_basis": "verified",
      "expected_disposition": "report",
      "fixability": "contextual",
      "notes": "Reproduce: click 'Subscribe to alerts', Tab into the dialog, then Tab/Shift+Tab/Esc — focus never leaves. A rule engine cannot see this; a manual/driven tier can → ●."
    },
    {
      "id": "widget-color-only",
      "sc": "1.4.1",
      "title": "Beach open/closed status conveyed by dot color only — no text or icon alternative",
      "severity": "moderate",
      "where": "ul.status-list .dot",
      "visible_text": "Pillar Point / Half Moon Bay / Montara",
      "detectability": "manual-deterministic",
      "engine_detectable": false,
      "honest_basis": "flagged",
      "expected_disposition": "report",
      "fixability": "contextual"
    }
  ],
  "decoy_not_a_finding": [],
  "expected_conformance": {
    "verdict": "fail",
    "why": "2.1.2 (keyboard trap) is a Level A failure. The page is engine-clean, so an engine-only arm that reports 'no violations' and/or 'conformant' is over-claiming.",
    "must_handoff_scs": []
  }
}
